program AligmentProject;

uses
  Forms,
  UnitAligment in 'UnitAligment.pas' {Aligment};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TAligment, Aligment);
  Application.Run;
end.
